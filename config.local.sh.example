#!/usr/bin/env bash
# Copy this file to config.local.sh (gitignored) and fill in your
# remote OpenBSD build host. build.sh sources config.sh then, if
# present, config.local.sh -- so anything set here overrides the
# defaults.

REMOTE_HOST=203.0.113.10
REMOTE_USER=root
REMOTE_PORT=22
REMOTE_DIR=/root/openbsd-builder
